#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcmp - comparing strings
 * @s1: a string
 * @s2: a string
 * Return: result
 */

int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
