#include <stdio.h>

/**
 * Description: main - print the alphabet.
 * Return: 0 if success
*/

int main(void)
{
	int c;

	for (c = '0'; c <= '9'; ++c)
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}
