#include "main.h"

/**
 * main - prints the name of the program
 * @argc: argc
 * @argv: argv
 * Return: 0 if success
 */

int main(int argc, char *argv[])
{
    printf("Program name: %s\n", argv[0]);
    return (0);
}
