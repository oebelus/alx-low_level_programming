#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - the length of a string
 * @s: a string
 * Return: the length if success
 */

int _strlen(char *s)
{
	int x = strlen(s);
	return (x);
}
